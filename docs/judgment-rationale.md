# `rules/20-judgment.md` 各判準的來歷與理由

> 常駐區只留判準、邊界與一組正反例（格式規則見 `<REPO>/skills/maintain-guideline/SKILL.md` §5「常駐判準的格式」）。訂條理由、事故脈絡、修辭句與鄰條分工從常駐區搬到這裡，**原句保留不改寫**；標題與 `20-judgment.md` 的節名／補充判準名一一對應。要查「這條為什麼存在」時再讀，不會自動載入。例外：檔尾「2026-09-21 session review：非 20-judgment 檔的調整理由」一節收 `agents/worker.md`、`rules/10-dispatch.md`、`CLAUDE.md` 的理由。

## §1 何時該升級能力層

以下原句因 2026-09-09 常駐格式瘦身從 §1 移出，原句保留：

> 本節是升級門檻的 canonical 定義。

> 模型 mapping 見對應平台 dispatch 文件（Claude：`10-dispatch.md`；Codex：`../codex/rules/10-dispatch-codex.md`）。

> 已在高能力層時，換 fresh context、取得第二意見或重新定義問題通常比只加 effort 有效。

> 缺 log、repo 事實或環境量測時先補證據，不靠升級模型猜。（常駐區保留的判準已簡化為「缺證據先補證據」，此處保留原本列舉的三種證據類型：log、repo 事實、環境量測。）

## §2 補充判準（重構／字串抽取／搬移／更正事實宣稱類任務）

以下原句因 2026-09-09 常駐格式瘦身移出：

> （build-tool／依賴遷移尤其常在發版當下才爆）

## §2 補充判準（標「未驗證」之前）

以下原句因 2026-09-09 常駐格式瘦身移出：

> 標錯地方會留下一個帶著合規標籤、卻沒人打算補的破洞。

## §2 補充判準（修正與驗收輪次）

以下原句因 2026-09-09 常駐格式瘦身移出（原為 2026-09-07 升級的事故脈絡）：

> 驗收中途改文件，下一輪就專挑你上一輪新寫的句子（2026-09-07：三輪 9 則 FAIL 有 8 則是散文）。

2026-09-15 加「delta 輪不換新探測形狀」的理由：量 2026-09-12～09-15 兩個專案 6 個平行批次的 session jsonl，15 次首輪驗收 12 次 OPEN，三個安全檢查器切片（#40、#21、#25）全撞三輪上限。三輪的 FAIL 是三個不同的新形狀（#25：`.//unit/` → 大小寫 → `.d.ts`；#40：regex 弱化 → decoy 繞過 → 針對測試檔名的特例），verifier 自己註明第 2 輪的大小寫繞過「base 就放行，不是本輪迴歸」，第 3 輪的 `.d.ts` 是第 3 輪才加的驗收條件。舊句「影響本次安全邊界的問題不得藉 delta 排除」對安全檢查器這類產出等於沒有縮小範圍——每個新形狀都是安全邊界，每輪 verifier 跑幾十萬例 fuzz（第 2 輪 248,226、第 3 輪 559,240）必然挖到新的。實作在整批 wall time 只占 10–16%，驗收＋修正迴圈占 85%；一個修正迴圈（worker 修 16–27 分鐘＋verifier 9–15 分鐘）約 25–40 分鐘。發現本身是真的，所以改成開 issue 而不是丟掉。

2026-09-21 新增「同一波次散文合批」（量測窗 2026-09-18 09:26～09-21、38 session／230 派工）：77 輪驗收有 23 輪是文件類、其中 15 輪 OPEN；session 14fc3eb6 三次獨立 findings 文件驗收（09-20 03:03／04:41／08:02）全 OPEN，各接一輪修正 worker，顯示同一波次的散文產物逐 issue 分開驗收會放大修正輪次。平行批次例外是驗收時補的：合批若套到平行切片，會重現該 skill 記載的 2026-09-14 白等 55 分鐘。

2026-09-21 更正（同日 Codex review 後）：上段「顯示同一波次的散文產物逐 issue 分開驗收會放大修正輪次」撤回——14fc3eb6 那三份 findings 是有依賴的序列交付，三次驗收之間使用者兩度指示 merge 後繼續（09-20 03:17「開 pr merge，merge 後照你的順序做下去」、05:32「merge 繼續」），照原句合批會擋住 merge。三輪全 OPEN 的觀察保留；判準改為有條件採用（證據穩定、無個別交付依賴、不得延後已可交付項），正反例一併更換。

## §2 補充判準（把可證偽宣稱寫下來之前）

以下原句因 2026-09-09 常駐格式瘦身移出：

> （其觸發條件已涵蓋「要報數字之前」）；這裡只留判準，

## §2 補充判準（「另一層會擋住它」不能只靠推理）

以下原句因 2026-09-09 常駐格式瘦身移出：

> 一般的錯了是文件不實，這個錯了直接變成漏洞。

## §2 補充判準（改既有檢查／過濾／驗證規則）

以下原句因 2026-09-09 常駐格式瘦身移出（含 2026-09-06、2026-09-09 兩次升級寫入的說明性文字）：

> 「跑測試全綠」不算：測試只覆蓋你改的那條路，「舊版擋得住而新版放行」的案例沒有人寫過，全綠與什麼都沒驗到長得一樣。

> 事後補的基線量的是新版自己。

> **方向不同，證據不同**：上面那組證據只擋**收緊**時的誤叫。

> **放寬**時基線與正向語料照做（它們擋意外變化），但**不足以結案**——放寬的差異本來就是刻意的，基線只證明「沒有意外變化」，不證明「沒有新洞」。

> 放寬安全檢查**另必派 fresh-context 對抗性探測**（攻擊者視角：別名、參數遮蔽、`this`、模板字面值等改寫，派 general-purpose 依 `10-dispatch.md` §1），且判不出是否安全的輸入維持擋——「證明安全才放行」，不是「認得出危險才擋」。

> 「證明安全才放行」，不是「認得出危險才擋」——2026-09-09 lesson 明確標出的對照句（見 `<REPO>/rules/50-lessons.md` 或 `<REPO>/docs/lessons-archive.md` 對應條目）。

> 「順手動到既有檢查」不算全新。

## §3 何時該停下來問使用者

2026-09-21 新增「本人在場」（量測窗 2026-09-18 09:26～09-21、38 session／230 派工）：session ba9bb6f5 09-21 02:49 派出等待 live 登入的背景指令後逾時，使用者 08:48 才回「沒有登入」（約 6 小時空轉）；另有 10.9 小時花在兩次沒人回的 AskUserQuestion，顯示需要使用者本人在場的動作應先問可用時段再派。

2026-09-21 更正（同日 Codex review 後）：補「其餘已授權且不依賴使用者的工作照常繼續」，避免「先問時段」把整個任務停住；依據是上段 10.9 小時花在沒人回的 AskUserQuestion。

## §7 現況證據與規格權威分工

以下為 2026-09-09 歷史背景；現行判準已於 2026-09-14 經使用者同意改為現況證據與規格權威分工，以 rules/20-judgment.md §7 為準。

使用者原話（2026-09-09）：

> Single Source of Truth 就只有 Code，文件永遠都只是參考，這件事情一定要讓 AI 知道，而且最好反覆強調，因為只要 Code 跟文件產生衝突，最後在運作的東西還是 Code，不是文件。
> 寫 Markdown 根本不只是為了什麼 Spec、規格文件、開發流程而已，更重要的是把留在對話討論的東西存檔，讓 AI 知道「想補對這個專案的認知」時知道去哪裡看。
> 以及例如為什麼這個東西這樣設計、哪些地方不能亂動、過去踩過什麼坑、現在有哪些邊界條件、做決策時應該遵守什麼原則邊界，這些東西如果全部只停留在對話裡面，早晚都會被 Context 洗掉、壓縮掉、稀釋掉、提醒到痛不欲生。
> 不然今天跟 AI 講得再清楚，過幾十輪之後，早就歪到姥姥家了，我覺得用這個角度去想之後，反而才能真的好好 Focus 在 Spec 要寫什麼，然後也不會過度在思考要怎麼把 Code 完全完整同步翻譯成人話文件，這件事不如讓 AI 讀 Code 就好。
> 很多人都搞錯方向，變成多維護一份完整的 Code 翻譯文件。

補充：註解同理——不需要寫太多，在不好理解的地方、有商業決策的地方再補；整體設計方向以另外的文件描述。反例引的數字來自 `<REPO>/docs/archive/verification-round-inventory-2026-08-29.md` §3.1（93 則 FAIL 中 40 則落在程式碼註解，43%）。

## 2026-09-21 session review：非 20-judgment 檔的調整理由

使用者 review 了 2026-09-18 09:26～09-21 共 38 個 session／230 次派工的實測資料後，明確要求五項調整；`20-judgment.md` 的兩項理由在上方對應標題下，其餘如下。

**`agents/worker.md` 規則 8「工具呼叫上限」——同日試過、撤回**：worker 工具呼叫中位 90 次／16.1 分，13/89 超過 150 次、8 個超過 40 分，最糟 345 次／51.8 分；派工 prompt 有無重申「指令批次化」沒差（中位 97.5 vs 89 次）。一度加上「超過 150 次未進最後 validation 就停止回報」，同日撤回：逐一檢視那 13 個，4 個是大批次機械改檔（Edit 82–152 次、13–27 分）、7 個是大功能實作（Edit 53–91 次）、2 個 Bash 偏重但仍有產出，沒有一個是鬼打牆；硬停只會打斷有產出的工作、再付一次冷啟動（第一次 Edit 前的摸索中位 24 次、占總呼叫 29%），且與 parallel-dispatch §3「不用單一 counter 當硬上限」相反、worker 也無法精確自計。鬼打牆由規則 7 管；尺寸由 controller 在核 plan 時管（見下方 `rules/10-dispatch.md` 段）。

**`agents/worker.md` 規則 9「工具呼叫次數」回報欄位**：同上量測窗，讓下次 review 看得到呼叫數分佈；成本近零，不作門檻。

**`rules/10-dispatch.md`「Controller 工作迴圈」第 1 步「ownership 不相交先判斷能不能切」**：最密集兩個 session（14fc3eb6、ff6135b4）最大並行度只有 2，只有 43% 派工與其他 agent 重疊；平行是使用者開口（ff6135b4 09-20 19:53）才啟動，顯示 controller 預設序列、沒有主動判斷可平行的切片。驗收時發現與同檔 §1 既有入口重複，常駐句改為指向 §1，只保留「不預設序列」。同日追加「大片先找平行切法、切不出來維持單片」：窗內 8 個超過 40 分鐘的 worker 都是整個功能一片到底；而序列拆小每片要再付一次冷啟動（摸索中位 24 次呼叫），只有平行才賺得回來，所以判準是找不相交切法而不是一律拆小。「約 40 分鐘」取自窗內長尾（8/89）。

**`CLAUDE.md`「## 語言」英文任務仍用繁中回覆**：760 則 controller 回覆有 37 則（5%）實質非中文，使用者糾正三次（e9e64834 09-19 07:10、07:36；a2a3ad15 09-19 01:25），都發生在任務內容是英文的 session。

**2026-09-21 更正（同日 Codex review 後）**：(1) `agents/worker.md` 規則 9 的「工具呼叫次數」回報欄位撤回——與上方「worker 也無法精確自計」及 parallel-dispatch「單片尺寸評估」的「不可從文字猜」矛盾；呼叫數由 controller 從 session jsonl 取得（本次量測即取自 jsonl），沒有可靠來源就標 `unknown`。(2) 「約 40 分鐘」從 `rules/10-dispatch.md` 常駐句移除：窗內 8 個超過 40 分鐘的 worker 都是整功能任務，推不出整功能任務多半超過 40 分鐘。(3) 「只有平行才賺得回來」撤回：序列交接可能減少 context 膨脹與重讀成本，改為比較冷啟動、context、重讀與交接成本，與「單片尺寸評估」一致。(4) 以上數據全來自 Claude 端；`codex/rules/10-dispatch-codex.md` 同日補的是同一流程原則，效能改善尚待 Codex 自身量測。

## 2026-09-22 KKBOX-Slim session review：Explore 前置與單片 context 提醒的理由

量測對象：KKBOX-Slim session `fecd2381`（2026-09-21 20:14～22:38 實際工作 2h24m；其後 6 小時是使用者空檔）。使用者開工 prompt 指定「單一片、實作用 opus」，controller 派 general-purpose opus 單片實作 #26 第 19 項（KKSL v2）。

**`rules/10-dispatch.md`「Controller 工作迴圈」第 1 步「核定前先派 Explore」**：implementer 開工前 8 分鐘用了 76 個 turn 讀碼（第 77 turn 才第一次 Edit；context 57k→292k，tool 執行只 0.3 分鐘），派工 prompt 已有 8.6k 字仍不夠讓它免讀——但那 8.6k 字是任務描述，不含檔案內容與既有測試結構，所以這條的假設是「附的是素材而不是描述」才省得掉摸索段；這是尚未實證的假設，檢驗方式：下次同類派工比「第一次 Edit 前的 turn 數」，沒降就撤回，不重演 worker 規則 8 的路徑。與 2026-09-21 review 量到的「第一次 Edit 前的摸索中位 24 次、占總呼叫 29%」是同一件事。Explore 只回結論、成本落在 sonnet 且不進主對話 context；「Explore／Plan 看不到 rules」目前是 `docs/harness-facts.md` 的候選觀察（issue #3），不是已核定事實，本段成本論據不倚賴它；素材附進 brief 後執行者的探索段可省，controller 也才有依據估單片 context。不另設 planner 角色：內建 `Plan` 已能出計畫，缺的是 controller 有沒有派，不是角色缺席。

**`skills/parallel-dispatch/SKILL.md`「單片尺寸評估」加 context 約 300k 提醒**：該單片首輪 4 commit（含兩輪修正共 7 commit、33 檔 +4781 行；首輪 4 commit 為 30 檔 +4119 行），首輪實作 50 分鐘（tool 執行 12 分、模型產出 38 分、含讀碼段共 268 turn／266k output tokens），時間面合理；問題在 context 一路漲到 655k（1M 視窗），336 turn 平均每輪重讀 426k、累計 143M cache-read tokens（同 session 主對話 24M、兩個 verifier 合計 24M）。commit 1–2（格式核心）與 3–4（saver／play 整合）之間有清楚的交接點且 3–4 依賴 2，平行切不了但序列交接可以，每段 context 可壓在 300k 內。300k 取「200k 以上進長 context 計費」與「交接冷啟動再付一次摸索」之間的折衷，只作提醒不作硬上限，與同段「不用單一 counter 當硬上限」一致。檢驗方式：之後 N 個單片交回時 `subagent_tokens` 的中位若 >300k，代表提醒值落在健康派工之下（與 60 次同形），即撤回。驗收兩輪 38 分鐘裡約一半是 .NET build+test 的 mutation 迴圈，屬「驗證不自驗」的必要成本，不列為問題。

**`skills/parallel-dispatch/SKILL.md`「單片尺寸評估」撤回 60 次工具呼叫提醒**：60 是 2026-09-17 訂的、當日 `<REPO>/docs/dispatch-cost-review-2026-09-17.md` 已標為待評估假設；2026-09-21 review 實測 worker 工具呼叫中位 90 次、13/89 超過 150 次且逐一檢視無鬼打牆（見上方「規則 8」段）。提醒值落在中位數以下，超過半數健康派工都會觸發，等於沒有訊號；同段已規定 counter 優先用 context 大小、呼叫數只是輔助，worker 也算不準自己的呼叫數。§6 第 10 步「不以首輪 60 次單獨決定」同步改為「不以工具呼叫數單獨決定」。

## 2026-09-22 parallel-dispatch skill fable review 落地

使用者核准套用一份 fresh review（`skills/parallel-dispatch/SKILL.md` 與 `references/{claude-code,cli,worktree}.md`）的全部 24 條建議。以下記錄需要保留理由、事故數字或撤回史的項目；常駐區只留判準與邊界。

**`SKILL.md` §8 worker 停滯規則加「探索段不計」**：原句「兩次相隔至少 10 分鐘或該片預估工時的一半，取小者」把 ≤20 分的片門檻壓到 ≤10 分，容易在健康探索段誤殺。證據：第一次 Edit 前摸索中位 24 次、占 29%（中位 16.1 分 → 約 4–5 分鐘 git 零改動，2026-09-21 review）；KKBOX-Slim 單片第 77 turn／開工 8 分鐘才第一次 Edit（2026-09-22 review）；來源 commit `4368dd5`（09-15）無任何實測引用。改為「第一次 Edit 前的探索段不計」，能查詢狀態的 adapter 以活動訊號為主，git read-back 只作佐證；同步、無法中途查詢的 adapter（Claude subagent，見 `claude-code.md`）仍以 git 兩次 read-back 為準，門檻降級為未實測的參考值。探索段（第一個 git 改動之前）刻意不設上限：2026-09-21 review 逐一檢視 13/89 長尾 worker 無一鬼打牆，之前的 60 次與 10 分鐘門檻誤殺的正是這段；全程零改動的 worker 由 §4「實作類任務改動為零＝失敗」事後抓，代價是那一次空轉全額付掉。

**`SKILL.md` §6 第 10 步續用成本數字移出常駐句**：實測續用輪每 request 成本約首輪 1.9 倍、一輪續用修正約一個 fresh 修正 worker 的 1.7 倍（方向證據，兩組任務難度未受控），且半數 follow-up 送出時 context 已 >200K（`<REPO>/docs/dispatch-cost-review-2026-09-17.md`「2026-09-18 更正」）。常駐句改留一句「續用成本高於 fresh，數字見 rationale」。
